#include <stdio.h>
#include <stdlib.h>
#include "funcoes.h"
int main()
{
        /* 4)
    Escreva um procedimento que receba como parâmetro um
    inteiro N e imprima
    todos os divisores de N    */

    int Divisores(int a){
    int cont;
    cont = a;

    while(cont != 0){
        if(a % cont == 0)
            printf("[%d] ", cont);
        cont--;
        }
    }

    Divisores(12);
    return 0;
}
